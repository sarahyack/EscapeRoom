package utils;

/**
 * Priority enum for use in PriorityMessage, used to determine the priority of a message.
 * Values are in the order of LOW -> NORMAL -> HIGH
 */
public enum Priority {
    HIGH,
    NORMAL,
    LOW
}
